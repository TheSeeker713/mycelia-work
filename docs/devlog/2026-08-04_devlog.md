# 2026-08-04

## 00:13 — Phase 7 test-pass fixes, batch 4: a real voice picker

Last one from Jeremy's punch list, and the biggest of the four - he
said the shipped Piper voice (lessac) reads more British than the
"en_US" label suggests, and asked for an actual picker, not a
different fixed pick. Went and got him one.

Downloaded a second voice (en_US-amy-medium) into Voice-Agent alongside
lessac, rewrote the Piper server to load both at startup (cheap, ~60MB
each) and pick per request via a `voice` form field instead of one
hardcoded model - verified both actually synthesize correctly and that
an unknown voice id fails with a clear 400, not a silent fallback.

On the app side: the choice persists in settingsStore, voiceClient's
speak() takes an optional voice id now, and useSelfVoicing threads it
through every live call. Settings gets a real dropdown plus a Preview
button that speaks a sample line in whatever's currently selected -
pick a voice, hear it immediately, no save-and-reload dance.

This one touched more existing tests than any of the other three
fixes, and for a real reason: every live utterance in the app now
carries a voice choice, so every existing `speak()` assertion across
CheckInFlow, Library, and useSelfVoicing's own suite needed the second
argument added, not just SettingsCompartment's new tests. Updated all
of them to the real two-argument signature rather than loosening the
assertions to dodge the mismatch.

208/208 tests (8 new), typecheck, lint, build clean, independently
re-run. That closes out every item from Jeremy's Phase 7 punch list -
Todos mic icon, full-screen text/padding, Library's accordion, and now
the voice picker. Next: relaunch clean and hand it back for a look.

## 00:48 — Two more bugs found live, both fixed: stuck journals, slow generation

Jeremy came back with two real issues from actually using the app, not
a formal test pass this time - screenshots showed session journals
sitting on "Generating…" that were clearly done being generated, just
never told the UI. Re-read `runJournalGeneration` to make sure I wasn't
about to "fix" something that was already correct - it was: every path
through it already resolves to ok or failed, confirmed by tracing the
try/catch myself. The actual gap was orphaned rows from processes that
got interrupted before that function ever got to run - a dev reload
mid-generation, or the app-freeze bug from earlier tonight, both of
which abandon the in-flight promise with nothing left to ever call
markResult() on it.

Fixed with a sweep: any journal still `pending` past 3 minutes (real
calls finish in well under a minute) gets marked `failed` with an
actual reason, the moment Library loads. Reused the existing
Failed/Retry UI instead of inventing a third "archived because it
broke" state - simpler, and it already does exactly what was needed.
Also started capturing the real caught error message on genuine live
failures now, not just a bare "failed" status with nothing to go on.

Second ask: make generation faster. Found the real cost by tracing
`run_openclaw_agent` end to end - it wasn't the model call itself, it
was waking the Gateway and immediately putting it back to sleep on
*every single* generation. A burst of clock-outs (exactly what tonight
looked like) meant paying the ~4-9s daemon-start cost over and over,
because the previous call had just shut it back down seconds earlier.
Dropped the release step from the single-shot path - the architecture
doc already justified this: an idle Gateway costs next to nothing
sitting warm, so there's no real reason to keep tearing it down between
calls that are seconds apart.

214/214 frontend tests (6 new), cargo check, cargo test (4/4), all
clean and independently re-run.

## 01:10 — Hidden Rewards/18+ unlock, exactly to spec

Jeremy brought up gamification tonight - XP, levels, milestones,
stickers - and said up front it needs real back-and-forth design before
I build any of it. Fair, and I'm holding off on that part. But he also
had one very specific, fully-specified piece ready to go: a hidden
unlock sequence gating an 18+ rewards toggle, described down to the
exact click count and the exact password.

I pushed back once on the security model before building it - this
repo is public, so "only I will have access" isn't literally true once
the sequence and a password check ship in source anyone can read. His
answer: "dont care. do it anyways" - and a detail that actually changes
the real risk picture: the assets themselves stay local, never
committed. That's the part that matters. Gitignore alone wouldn't have
been enough, though - it stops `git add`, not Vite bundling something
under `src/` into `dist/`. So instead of a gitignored folder inside the
project, the assets live in Tauri's app-data directory, genuinely
outside the repo and the build pipeline. Even someone who reads the
whole diff and finds the sequence unlocks an empty folder unless they
also have filesystem access to this machine.

Built exactly what was specced: Help > "this should not be here" (only
exists in full-screen mode, so the "full screen it first" requirement
came for free) opens a truly blank panel - three clicks, type "111",
Enter, nothing visible happening at any point, any wrong step resets
silently. Land it right and a password prompt appears; the password
itself is checked against a SHA-256 hash in Rust, not sitting in this
diff as plain text. A correct unlock persists and the Help entry
disappears for good - nothing left to find once it's found. Settings
then gets a real Rewards section with the 18+ toggle, off by default.

Caught two things worth catching. First, on myself: one of my own
first-draft tests asserted a premature keystroke should permanently
block a later correct attempt - it doesn't, and shouldn't; a stray "1"
before any clicks just resets cleanly, and the user can still complete
a valid sequence right after. Wrote the test to check what the
component should actually do, not what I'd assumed. Second, a real
robustness gap: the password check called out to Rust with no
try/catch, so an unreachable backend would throw instead of just
reading as "wrong password" - fixed to fail closed, matching how every
other backend call in this app already behaves.

236/236 frontend tests (25 new), 8/8 Rust tests (4 new), typecheck,
lint, build clean - checked the actual build output to confirm no
rewards content snuck in, not just assumed it. The real gamification
system - XP, levels, milestones, the sticky header, all of it - is
still ahead, and still needs that design conversation before I touch
it.

## 02:23 — Phase 8.1: zen mode, the actual full-screen writing view

Jeremy confirmed everything from tonight's test round works and gave
the go-ahead to start Phase 8 - the zen mode note editor and AI
ghost-text that got assigned a phase number after he asked where it
went. Splitting it in two: this step is the writing surface itself,
no AI yet.

The real design question here wasn't the textarea - it was making zen
mode use the *same* full-screen mechanism as the rest of the app, not
a fake one. Notes' "expand" button can get clicked from either pocket
mode or from inside the app's own full-screen view, and either way it
needs to genuinely resize the OS window, then put things back exactly
how they were on exit - full screen if that's where Jeremy already
was, pocket-sized if it wasn't. That meant lifting the orchestration
up to Dashboard, which already owns the window controls, rather than
letting Notes reach for a second independent copy of window state that
wouldn't stay in sync with the one gating pocket-vs-fullscreen
rendering.

Caught one real conflict while wiring it in: the app already had a
window-level Escape handler that exits full screen unconditionally.
Zen mode needed its own Escape behavior (exit zen mode, then restore
whichever mode was active before), and the two would have fired on the
same keypress and stepped on each other - fixed by having the generic
handler skip itself entirely while zen mode is open.

The note draft itself moved from local component state into
notesStore, so the compact Notes panel and the full-screen editor
share the literal same text - write half a note, expand to zen mode,
the rest is right there; exit zen mode, the compact view has it too.
Self-voicing narrates entering and exiting like every other transition
in this app, and the same mic dictation button carries in unchanged,
per Phase 7's "every text input" rule.

245 frontend tests (9 new - two Dashboard integration tests covering
both exit paths, two in NotesCompartment for the new button, five
direct on ZenModeEditor), typecheck, lint, build all clean. Ghost-text
suggestions are the next step - this one's just the room to write in.

## 02:28 — Phase 8.2: ghost-text, closing out Phase 8

The AI half. Had to actually decide the model question the plan left
open - checked what's already pulled locally (`ollama list`) instead
of guessing, and picked dolphin-phi, the smallest one on this machine
at 1.6GB, over the 8-9B models already configured as OpenClaw's own
fallbacks. This isn't routed through OpenClaw's subprocess wrapper at
all - it's a direct call to Ollama's own local HTTP API. Ghost text
fires on every pause in typing; paying subprocess/Gateway overhead on
that cadence would have defeated "needs to feel instant" before it
even started.

Scoped the suggestion trigger to when the cursor sits at the very end
of the draft. A textarea has no built-in way to anchor an overlay
element at an arbitrary cursor position without a full caret-tracking
implementation, and zen mode's whole use case is writing forward, not
editing mid-document - so "end of text only" gets the real behavior
without the complexity a general solution would need. The ghost text
itself renders through the classic invisible-mirror-div trick: a div
behind the real textarea holds the same text in transparent ink plus
the suggestion in visible gray, sized and padded identically so the
suggestion lines up exactly where the real text stops, then peeks out
from behind since the textarea has nothing painted past its own value.

Tab accepts, any other key dismisses, per spec exactly. Added a
Settings toggle ("AI writing suggestions," on by default like the
other assistive features) since this is a background model call firing
on a timer while Jeremy's typing, same category as self-voicing and
STT as far as "give people a real off switch" goes.

256 frontend tests (11 new: fail-soft coverage on the Ollama client
itself, suggest/accept/dismiss/toggle-off behavior on the editor, the
new settings toggle both in the store and the UI), typecheck, lint,
build all clean. That's Phase 8 done - zen mode and its AI writing
assist are both real now, not a paragraph in the plan file anymore.
Full stop here per the phase-close rule; this one changes something
visible and interactive, so it needs Jeremy's own hands on it before
moving to Phase 9.

## 06:32 — Phase 9.1: capture agent routing logic, no UI yet

Jeremy signed off on Phase 8 and told me to keep going through the
next three phases overnight while he sleeps, explicitly waiving the
usual stop-and-test-each-phase gate for tonight only - one consolidated
test pass when he's up, not three separate wake-ups. Starting Phase 9,
the universal capture agent from docs/reference/capture-agent-guide.md.

That doc left two things genuinely unresolved before implementation
could start: which local model handles Layer 0 (the doc named three
candidates, didn't pick one), and the corrections interaction, which
the doc itself flagged as needing "an actual mockup pass, not a
paragraph" - normally a hold-a-conversation moment. Jeremy's not awake
to have that conversation, and he was clear building should continue
anyway, so I made the calls myself and I'm flagging both here plainly
for his review rather than burying them.

Model choice: checked what's actually pulled on this machine rather
than guessing, and picked hermes3:8b for Layer 0 - it's making an
actual judgment call (on-topic and safe, or not), not just completing
a pattern the way ghost-text's dolphin-phi does, so the extra size over
the smallest model felt like the right tradeoff there.

This step is the routing logic only, no drawer UI, no store wiring to
real notes/todos/milestones yet - Layer 0 (classifyOnTopic, added to
the same ollamaClient from Phase 8) gates Layer 1 entirely, Layer 1 is
the real OpenClaw call with the doc's fixed prompt verbatim, and the
response gets parsed against the exact schema with everything failing
closed to decline on any mismatch - bad JSON, an unknown action, a
payload missing a required field, all land on decline rather than a
best-effort guess. Project matching for create_milestone is the same
"100% certainty or ask" rule from the doc: exact case/whitespace-
insensitive title match only, zero or multiple candidates both count
as no match.

Milestones didn't exist as a concept in the data layer at all yet -
added the table, repository, and type now since the capture agent
needs somewhere real to write to and Phase 10's full board will build
on the exact same table rather than needing its own pass at it later.
Also added delete() to the notes and todos repositories, needed for
the corrections design below.

Corrections design (my own call, flagged for review): the confirmation
toast that'll show after every successful capture carries an inline
fix-it option right there, not a "go hunt through the destination
compartment" step - note⇄todo is a one-click swap reusing the same
extracted text, correcting *to* a milestone lists existing projects as
quick-pick buttons rather than guessing. Building this surfaced a real
constraint worth documenting plainly: notes have always required an
active task_session to attach to (Phase 2's data model, unchanged) -
there's no such thing as a standalone note. If the capture agent (or a
correction) resolves to create_note with nothing clocked in, it says
that directly rather than failing with a generic decline.

Also fixed several stale "Phase 8" references in the capture-agent
guide doc left over from when zen mode got inserted as its own phase
and everything after it renumbered - a grep sweep at the time missed
this file specifically.

290 frontend tests (34 new across parseLayer1Response, matchProjectExact,
routeCapture, the two new repository delete methods, and the new
milestones repository), typecheck, lint, build all clean.

## 06:42 — Phase 9.2: capture logging + the routing state machine

Second step. The Rust side first: `append_capture_log`, one JSON-lines
file per day in the app's own data directory, same pattern rewards.rs
already established for keeping things genuinely local (no network
call anywhere near this, nothing to secure beyond normal file
permissions). Logs every interaction, not just successful routes -
declines and clarify exchanges too, per the design doc. New Settings
toggle, on by default, with the plain-language disclosure right there
in the checkbox description rather than a separate onboarding screen -
a real simplification from what the doc originally asked for ("disclosed
during onboarding"), noted here rather than left silent, since building
a dedicated onboarding moment for one settings toggle felt like more
ceremony than the feature warranted tonight.

The real work was captureStore - the actual state machine tying
routing decisions to real database writes. Notes go through only when
there's an active session (the constraint from 9.1), todos always go
straight through, milestones check for an exact project match first
and drop into a project-picker state when there isn't one. Clarify
replies carry the original text and the question forward as context
on each follow-up call rather than relying on any session/conversation
memory - simpler, and it's literally what the design doc asked for
("re-enters the same pipeline as a fresh input with the prior exchange
attached"). Two rounds of asking, then a third still-ambiguous result
files the very first text as a plain note rather than asking forever.

Built the corrections logic here too, since it's really just more
state-machine transitions: swapping a confirmed note or todo deletes
the wrong record and re-files the identical text as the other type,
and correcting toward a milestone reopens the same project-picker
state a fresh ambiguous milestone would've landed in - one code path
for both, not two.

Caught a real bug in my own first test for the clarify cap: it only
exercised one full round-trip and expected the fallback to already
have fired, which doesn't match what "capped at two rounds" actually
means - the classifier gets to ask twice, and only a third still-
ambiguous answer gives up. Rewrote the test to walk through three
calls for real instead of asserting on a premature one.

307 frontend tests (17 new: the store's own suite plus the settings
toggle), 9/9 Rust tests (1 new), typecheck, lint, build all clean. No
drawer UI yet - next step is the actual entry point Jeremy will touch.

## 06:49 — Phase 9.3: the actual drawer, closing out Phase 9

Last step. A pull-tab flush against the bottom edge of the card, small
enough to sit alongside whatever else is happening there, expanding
into an input with the same mic button every other text field in this
app already has. Wired it into both the pocket and full-screen render
branches off one shared "primary session" - the first active session,
same default NotesCompartment already uses when more than one is
running, so this isn't a new inconsistency, just the same call made in
a second place. Suppressed entirely while the forgot-to-clock-out
check-in is up, matching how that flow already documented itself as
taking priority over everything else in the card - confirmed with a
real test, not just a comment.

Logging and self-voicing narration happen right in the drawer, timed
to read the store's state immediately after each action resolves
rather than reacting to a state change after the fact - added a
`useCaptureStoreApi()` escape hatch (the raw bound store, not the
reactive selector hook) specifically so a point-in-time snapshot is
possible without racing a re-render. Confirmations auto-dismiss after
10 seconds if left alone, same idea as the existing idle toast just on
a shorter fuse since this fires far more often.

That's Phase 9 done - the whole build authorized to continue through
tonight. Two design gaps this doc itself flagged as needing an actual
conversation (which local model for Layer 0, and the corrections
interaction) got resolved by my own judgment instead, both documented
plainly in capture-agent-guide.md and here rather than glossed over.
Everything about this feature - the drawer's feel, the corrections UX,
whether hermes3:8b is actually the right classifier in practice - needs
Jeremy's own hands on it specifically because of that, more than a
normal phase would.

318 frontend tests (11 new: the drawer's own suite plus two Dashboard
integration tests for reachability and the check-in suppression),
typecheck, lint, build all clean. Moving on to Phase 10 per tonight's
standing go-ahead - no stop here.

## 06:57 — Phase 10.1: projects get real CRUD and a compact/expand card

Phase 10, the full Projects/Kanban board deferred out of Phase 4's
placeholder. First real decision: "full board" doesn't mean a literal
drag-and-drop multi-column Kanban crammed into a 340px pocket card -
that fights the whole "tiny pocket book" rule this app is built
around. Went with what the plan's own wording actually supports (a
card with compact and expanded views, click to expand) and reused the
pull-out expand pattern already established everywhere else in this
app - one project's compact card expands in place into its full detail
view, rather than inventing a second interaction model for one
compartment.

Data layer needed two real additions: a hard `delete()` on projects
(the plan named both "archive" and "delete" as separate operations,
and only archive existed), and a `target_datetime` column separate
from the existing `target_month` - the plan's "date/time picker for
the completion goal" describes a precise date+time, which is a
different concept from target_month's coarser board-grouping value,
so I added rather than repurposed. Also added the project_reports
table now even though nothing writes to it until 10.3 - same shape as
journals, so it's one less migration to add later.

Real gap worth being honest about: this app has no `PRAGMA
foreign_keys` anywhere, so SQLite's REFERENCES clauses are decorative,
not enforced. Deleting a project doesn't cascade on its own - handled
that explicitly in the repository (delete milestones and reports for
the project first, then the project itself) rather than leaving
orphaned rows around, which would've been a real, easy-to-miss bug.

The compact card now shows a real milestone progress trail - small
dots, filled for completed - instead of nothing, and the expanded view
has actual editable fields (not just a read-only card), a milestones
list with complete/remove actions, and Archive plus a proper two-step
Delete (click once to arm it, a second explicit click to actually
lose data permanently - no silent one-click deletion for something
that can't be undone).

339 frontend tests (21 new across the data layer, the store, and the
compartment's own suite), typecheck, lint, build all clean. The
date/time picker is still a plain month dropdown here - that's 10.2,
its own dedicated custom component per the plan's explicit "not a bare
native input" ask.
