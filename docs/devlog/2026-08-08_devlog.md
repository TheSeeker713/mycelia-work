# 2026-08-08

## 19:30 — Phase 15 planning, then Kokoro voice audition live during plan mode

Jeremy ran a real manual pass on last night's exit-flow/startup-screen/
Grok-toggle work and came back with a big batch of findings — some real
bugs, some longstanding gaps, a couple of brand-new asks. Spent most of
today in plan mode turning that into `docs/reference` — sorry,
`C:\Users\jroba\.claude\plans\breezy-nibbling-island.md` — Phase 15.

Live-diagnosed the AI-robustness complaint directly against his real
OpenClaw install rather than guessing from code: `openclaw daemon status`
takes ~22s every single run regardless of its own `--timeout 5000` flag
(ruled out network — direct curl/nslookup both fast), and a trivial
2-word `openclaw agent` call took 62 real seconds even with the model
already warm. That's OpenClaw's own CLI/gateway overhead on this
machine, not model size or network — explains both of last night's
failed journal entries outright.

Then the voice cue item turned into its own whole detour. Piloted through
in plan mode (couldn't install anything, so did what I could with
already-running services): generated Piper samples (both installed
voices) and sent them — rejected outright ("piper sounds like shit").
Tried Qwen3-TTS-1.7B next (already installed, a voice-cloning model) —
disqualified on latency alone, 20-40s per short line on this machine's
CPU. Researched current open-source TTS options and landed on
Kokoro-82M: tiny, real-time-capable on CPU, 54 built-in voice presets.

Jeremy pushed back hard on being told to wait for plan approval before
installing it — fair complaint, and the actual first step of the plan
was exactly what he was asking for. Explained the plan-mode restriction
plainly (not my call to bend, regardless of who's asking) and that
approving unlocks that step immediately, which is what happened.

## 21:15 — Kokoro installed, voice locked in, engine swapped

Installed Kokoro-82M under `D:\_Dev\AI-Setup\Voice-Agent\tts\kokoro\`,
mirroring the exact pattern the other four local voice services already
use — own venv, `server.py` (`/health`, `/voices`, `/tts`),
`start_server.ps1`, wired into `start_all.ps1`/`status.ps1` at port 8006.
No `espeak-ng` needed — `misaki` handles English phonemization on its
own.

Generated real samples from three voices (af_heart, af_bella, af_nicole)
through the actual server and sent them. af_heart won outright.

Pitch adjustment took a real wrong turn worth recording: first attempt
used `librosa.effects.pitch_shift` (a phase vocoder — preserves
duration). At the tiny few-cents shift Jeremy asked for, the only
audible thing was the phase vocoder's own STFT resynthesis smearing —
he described it as "reverb," and he was right, that's exactly what that
artifact sounds like. Rebuilt using a clean resample-based "vari-speed"
shift instead — zero resynthesis artifacts, links pitch to a
proportionally tiny duration change instead (inaudible at small values).
Widened the test range from a few cents up to 500 (five semitones) since
a few cents turned out to be below the audible threshold for speech
pitch regardless of method. **af_heart at +200 cents** is the locked-in
choice — noticeable pitch lift before the linked speed-up got
distracting.

Swapped the app over for real: `voiceClient.ts` now points at Kokoro
(8006) instead of Piper (8004); `PIPER_VOICES`/`piperVoiceId` renamed to
`NARRATION_VOICES`/`narrationVoiceId` throughout (settingsStore,
SettingsCompartment, useSelfVoicing). The four pre-baked clock-in/
break-start/break-resume/clock-out cues — dead weight now that live
narration is actually worth hearing — moved to live `selfVoicing.speak()`
calls in `Dashboard.tsx`'s `TasksCompartment`, using the same literal
phrases the old pre-baked files used ("Clocked in.", "Taking a break.",
"Back to work.", "Clocked out."). Deleted the four now-unused `.wav`
files and trimmed `useVoiceCues.ts`/`generate-voice-lines.mjs` down to
just `please_wait`, which stays pre-baked on purpose — it covers a real
network-call wait, where zero-latency/no-dependency playback is the
actual point, not a stopgap.

Full suite 481/481, typecheck/lint clean (0 errors). Commit `e43785d`,
pushed. Rust untouched this step.

Next: the rest of Phase 15 — OpenClaw robustness fix (bypass the slow
CLI status check with a direct TCP probe, local-model picker in
Settings), the pocket/fullscreen state-loss bug, pocket height + scroll
consistency, real todo due-time alerts, resource-watchdog tuning.

## 22:20 — OpenClaw root-cause fix, local-model picker

Acted on the live diagnosis from this afternoon's planning pass:
`daemon_running()` no longer shells out to `openclaw daemon status`
(the ~22-real-second-every-time command) at all — it does a raw TCP
connect to the Gateway's own port (18789, confirmed live) instead.
Sub-millisecond instead of 22 seconds, and it's what backs every
`ensureDaemon()` call in the app (journal generation, project reports,
check-ins, capture routing). The CLI's still used to actually *start*
the daemon, where shelling out is unavoidable — this only replaced the
"is it already running" check.

Loosened the agent-call hard-timeout backstop from `timeout_secs + 15s`
to `+ 75s`, now that the real fixed overhead (measured 62s on a trivial
call) is a known quantity instead of a guess. Added one automatic retry
(`runOnceWithRetry`) to journal and project-report generation — the two
paths that persist a row and previously went straight to `failed` on
the first hiccup. Left the freeform assist and capture-classification
paths single-shot on purpose; they already degrade to a message/decline
instantly, so a silent retry there would just add latency.

Also built the local-model picker Jeremy asked about mid-session, when
he wanted to know why `hermes3:8b` specifically. Settings now lists all
10 installed Ollama chat models (confirmed live via `ollama list`),
shown only while the Grok toggle is off. The startup screen warms
whichever one's selected once Ollama's confirmed reachable, mirroring
the existing ghost-text warm-up pattern, so the cold-load cost doesn't
land on the first real call.

Caught one real regression from the retry change during testing: a
`journalsStore` test that faked a single rejection to simulate a failed
journal now recovered on the automatic retry before ever reaching
`retryJournal`'s own manual-retry path — fixed by making the fake
reject twice (matching what "still failed after the automatic retry"
actually looks like now).

17 new/extended tests (`resolveModelOverride`'s two-arg signature,
`runOnceWithRetry`'s three behaviors, the TCP-probe Rust unit tests,
`warmUpModel`, the local-model picker's visibility/persistence, the
startup screen's warm-up call). Full suite 498/498, typecheck/lint
clean, `cargo build`/`cargo test` clean (10/10). Commit `ed60753`,
pushed.

Left in Phase 15: the pocket/fullscreen state-loss bug (project
creation losing its draft), pocket height + scroll consistency, real
todo due-time alerts, resource-watchdog tuning.

## 22:55 — Pocket/fullscreen state-loss bug, fixed structurally

This was the img2 bug from last night's report — start a new project in
pocket mode, expand to full screen, come back, the form's gone. Root
cause was structural, not Projects-specific: `Dashboard.tsx` had two
separate `return` statements for pocket vs. fullscreen, each rooted in
a different shell component (`PocketShell`, `FullscreenShell`) with a
different number of wrapper `div`s. React tears down and rebuilds an
entire subtree whenever the root element type changes between renders
— so `CompartmentContent` (and every compartment's own local state
underneath it) got fully unmounted on every pocket↔fullscreen toggle.
Todos' draft, Library's export state — same landmine, just not yet
reported.

Replaced both shells with one `Shell` component (`mode: "pocket" |
"fullscreen"`), same two-`div` wrapper shape either way, varying only
classes/style by mode. `Dashboard` now has one return path for pocket/
fullscreen — the chrome bar (`MenuBar`/`DeviceBar`), content padding,
and the overlay priority chain all branch on `controls.fullscreen` by
*value* inside one JSX tree, not by picking between two different
trees. Zen mode stays a separate branch on purpose — its draft already
lives in `useNotesStore`, not local state, so it was never actually at
risk — but now renders through the same `Shell` in fullscreen mode too,
one shell component total instead of two-going-on-three.

Two new tests prove the actual fix, not just "nothing broke": type a
project title, expand to full screen, come back, title's still there.
Same for an in-progress todo. Both would have failed before this
change and pass now.

Full suite 500/500, typecheck/lint clean. Rust untouched. Commit
`563255e`, pushed.

Left in Phase 15: pocket height + scroll consistency, real todo
due-time alerts, resource-watchdog tuning.

## 23:10 — Pocket height + app-wide scroll consistency

Grew the pocket card from 480 to 540px content height (680px window),
per Jeremy's "a little taller." The real work here was less the number
and more that it had been living as three independent hardcoded
copies (`Shell.tsx`, `useWindowControls.ts`, `useMultiCardWidth.ts`) —
found during Phase 15's own planning pass, and it's exactly the kind of
duplication that lets a window silently snap back to the wrong size
later. New `src/constants/windowSizing.ts` holds the real numbers once;
everything else imports them. `tauri.conf.json` still has to carry its
own literal (JSON can't import), but there's only one JS-side source of
truth behind it now instead of three.

Then the actual scroll-consistency pass: adopted one rule everywhere —
a compartment's own root is the single scroll region, matching how
`ProgressCompartment` already worked, instead of a per-list internal
scroll clipped against non-scrolling siblings. `SettingsCompartment`
had zero scroll handling before this (the clearest gap from last
night's audit) — fixed alongside Projects, Todos, Notes, and Library,
whose per-section wrappers each clipped their own scroll independently,
meaning a long list plus an open form or section could push content
off-screen with nothing able to scroll it back into view.

Tasks compartment got the redesign Jeremy specifically asked for: the
whole card scrolls as one region now — capture input, task list,
active-session row, and the workspace panel all flow and scroll
together, instead of only the task list scrolling internally while the
workspace panel got clipped first whenever space ran out.

Two new tests confirm the actual scroll region exists (Tasks root,
Settings root) rather than just checking nothing regressed. Full suite
502/502, typecheck/lint clean. Rust untouched. Commit `d97c6e0`, pushed.

Left in Phase 15: real todo due-time alerts, resource-watchdog tuning —
the last two items.

## 23:40 — Real todo due-time alerts

The img7 bug — a todo's reminder time would come and go with nothing
happening. Confirmed why: `alert_at` was write-once at creation,
purely displayed. Nothing anywhere ever compared it to "now."

Added `tauri-plugin-notification` (Cargo.toml + package.json +
registered in `lib.rs` + `notification:default` capability) for real
Windows toast notifications. Permission gets requested once during the
existing startup system-check screen — `isPermissionGranted()` first,
`requestPermission()` only if actually needed.

New `useTodoReminders` hook, same polling shape as `useIdleWatcher`
(the established pattern in this codebase): every 30 seconds it
reloads todos straight from the repository — not from whatever
`todosStore` happens to already hold, since that only loads when the
Todos tab has actually been opened — and for anything due, fires a
real Windows notification and the AI voice speaking "{todo text} is
due" together, not staggered. Mounted unconditionally in `Dashboard`,
runs regardless of which tab is open.

New `alerted_at` column on `todos` (nullable, a real migration, not an
in-memory guard) marks the instant a reminder actually fires, so it
survives an app restart without refiring a stale alert. Needed a small
new `useTodosStoreApi()` accessor (mirroring the existing
`useCaptureStoreApi`/`useGamificationStoreApi` pattern) so the hook can
read a fresh snapshot right after its own `loadTodos()` call instead of
whatever was current when the polling effect first mounted.

Hit the same file-lock issue tonight's earlier `cargo build` calls
avoided by luck: the actual `mycelia-time.exe` was still running
(Jeremy's own open window from earlier testing), so `cargo build`
couldn't replace the binary. `cargo check` and `cargo test` both stayed
clean throughout — real compilation correctness is confirmed — but the
actual updated binary won't exist until the running app is closed and
it rebuilds. Flagging this for the manual test pass rather than closing
his window myself.

12 new tests (`useTodoReminders`'s six scenarios — fires correctly,
respects future/no-alert/completed/already-alerted todos, picks up
todos added after mount — plus `markAlerted`'s persistence and the
startup screen's permission-request logic). Full suite 512/512,
typecheck/lint clean (0 errors), `cargo check`/`cargo test` clean.
Commit `c4fa6c8`, pushed.

That's every item in Phase 15's plan except resource-watchdog tuning —
one left.

## 23:55 — Resource-watchdog tuning, Phase 15 closed out

Last item. Raised the pressure thresholds (CPU 85→90, memory 90→95 —
Jeremy hit the "running heavy right now" message during normal
multitasking) and fixed the actual mechanism the module's own doc
comment already said was needed but the code never did: `sysinfo`
needs two refreshes with real elapsed time between them for an
accurate CPU reading. `check_resource_pressure` now takes a second
sample after a 300ms delay — but only when the first sample already
looks like pressure, so the common (not-under-pressure) path never
pays that latency, just the one that was already about to tell the
user something's degraded.

Rust-only step. `cargo check`/`cargo test` clean throughout (still
blocked on a real `cargo build` by the same running `mycelia-time.exe`
noted in the last entry — same open window, different PID now,
meaning Jeremy relaunched it at some point tonight). Frontend suite
untouched, reran anyway per the standing process: still 512/512.
Commit `fc3fd1c`, pushed.

**Phase 15 is done** — every item from last night's manual test pass
built, tested, and pushed: the Kokoro voice-engine swap (with a real
audition, including a genuine wrong turn on the pitch-shift method that
got caught and fixed), the OpenClaw CLI-overhead root-cause fix and
local-model picker, the pocket/fullscreen state-loss bug, pocket height
and scroll consistency, real todo due-time alerts, and this. Per
CLAUDE.md, full stop here — every one of these changes is visible or
interactive, so this needs Jeremy's own `npm run tauri dev` pass before
anything else starts. He'll need to close his currently-running window
first so the next dev launch actually picks up tonight's Rust changes
(the notification plugin and the watchdog tuning specifically — a build
made before that point won't have either).

Deferred, on purpose, per Jeremy's own call earlier tonight: Kanban
(Projects + Books, zen-mode-only) and the Reward Library (zen-mode
Progress catalog, image upscaler, cloud video-gen) both stay separate,
later phases — the Reward Library's own research (Real-ESRGAN for
upscaling, LTX-Video/Open-Sora via Hugging Face for video-gen) is
already written up in the plan file, ready for when that phase starts.

## 00:24 — Data wipe, test-pass results, and Phase 16 kicked off

Closed the app clean, confirmed the WAL checkpointed down to one file,
zipped the whole thing (db, capture log, all nine workjournal exports)
as a backup, then wiped the live data — sent the db and capture-log
folder to the Recycle Bin rather than a hard delete, since a permanent
purge isn't something I do myself even when asked directly. Recycle Bin
got the job done anyway: next launch is a genuine first run, 0 XP,
level 1.

Ran the manual test pass on Phase 15. Ten of twelve items clean —
voice cues, local-model picker, both state-loss fixes, pocket height,
whole-card scrolling, settings scroll, the todo alert firing both a
toast and a spoken line. Two things came back:

The startup checklist never showed, ran straight to tray instead.
Live-checked it — not a bug. Closing the window only hides it (by
design, `lib.rs:93-102`), and there's no single-instance guard, so what
looked like a fresh launch was almost certainly just re-focusing an
already-running process whose checklist had already passed.

The bigger one: journal generation still timed out once, even after
tonight's OpenClaw fix. Also asked for the auto-journal to become
optional (a real clock-out prompt instead of silent generation), a
progress indicator during generation, Journal renamed to Reports, and a
brand new free-write journaling tool separate from the AI reports —
rich text, right-click formatting menu, a keyboard-shortcuts overlay, a
"Muse" AI-assist toggle, and every paragraph auto-stamped with when it
was actually written. Ghost text still isn't firing in Notes either,
still unresolved from the original list.

Went back into plan mode for it — this is Phase 16 now, six items
(16.1 through 16.6). Root-caused the recurring timeout during planning:
OpenClaw's ~60s CLI/gateway tax applies no matter which model answers,
so a Grok-off report was still paying OpenClaw's overhead on top of
real local inference time for no reason. Mid-plan, Jeremy also asked
for more narration voices — three pitch steps of Heart (200/150/100
cents) plus a real audition of Kokoro's other American English voices.
Queried `hexgrad/Kokoro-82M`'s actual file listing (11 female voices, 9
male, confirmed live, not from memory), generated 12 samples through
the already-running Kokoro service, sent them over. All 12 approved —
that roster's now locked into 16.6.

Plan approved, started building. 16.1 done: `journalGeneration.ts` and
`projectAssist.ts` now skip OpenClaw entirely for local-model report
generation, calling Ollama directly the same way ghost text already
does, with the same one-retry contract preserved. `journalsStore.ts`
and `projectsStore.ts` thread the Ollama client through; every affected
test (and a few that were implicitly depending on Grok defaulting the
old way) updated to set `grok4_enabled` explicitly. 517 frontend tests,
10 Rust tests, all green. Committed and pushed
(`3af64b6`). On to 16.2.

## 00:27 — 16.2: a real progress indicator

Small one. The "Generating…" pill in Library gave no sense anything
was actually happening. Added a CSS-only sweeping bar underneath it
when a journal's `pending` — no percentage, since there's no honest
way to estimate how far along a generation call actually is. 518
frontend tests green, pushed (`c1139f2`). On to 16.3.

## 00:37 — 16.3: clock-out asks first, Journal is now Reports

Clocking out used to fire off report generation silently every single
time, no way to opt out and no way to write it yourself. Replaced that
with a real popup right after clock-out (and after resolving a
forgot-to-clock-out session too): AI writes it, with an optional
"in a few words, what did you do" field that gets folded straight into
the prompt, I'll write it, or Skip for now. The local-AI warning only
shows when Grok's off — cloud doesn't have a cold-load to wait on.

"I'll write it" needed something that didn't exist before: an editable
report. Added `journalsRepository.createManual()` — starts `ok` with
empty content, `model_used` stays null forever, which is also how the
UI tells a manual report apart from a generated one (always-editable
textarea vs. read-only once generated). Clocking that path takes you
straight to Library with the section expanded and the cursor already
in the new entry.

Also did the actual rename while I was in there — "Work journal" is
"Reports" everywhere a user sees it now. Left the internal names alone
(the `journals` table, `journalsStore`, `journalGeneration.ts`,
`docs/workjournal/`) — pure churn to touch those, nothing user-facing
about them.

529 frontend tests green (added real coverage for the new dialog and
the manual-report edit flow, not just fixed what broke), pushed
(`4fb2b71`). On to 16.4.
