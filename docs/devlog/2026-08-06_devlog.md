# 2026-08-06

## 16:00 — Scope change: private build, desktop only, handing off to Cursor

Jeremy decided this stays a private, personal build instead of the
public repo it started as - he's passing the next build wave to Cursor
while credits here recover, and confirmed the app is desktop only, no
Android companion app. He's handling the actual GitHub visibility flip
himself.

Copied the plan from the last long planning session (11+11 unlock,
gamification core, AI-backend transparency/pluggability, auto-update)
into `docs/reference/next-build-wave-plan.md` so it travels with the
repo instead of living only in a local Claude Code plan file. Struck
the Android companion app section and marked it cut, added a handoff
note at the top flagging that Phase B of the AI-backend work assumed a
public repo and needs re-checking against the new private-build call
before anyone builds it. Committed and pushed as `6cf6059`.

No code changed. Badge/asset curation from the 04th is still Jeremy's
own ongoing work, untouched.

## 18:12 — Credits back, default voice to Amy, CLAUDE.md reflects private-only

Credits recovered. Jeremy also revised the asset pipeline himself -
frog content pulled for now, replaced with anime-style characters, and
a `README.md` landed in `assets/gamification/` with a full content
disclosure (PG-13 to R, all characters 20+, no pornography, private
repo only). Didn't touch that pipeline, just read the README for
context.

Switched the default narration voice from Lessac to Amy - Amy's been
sitting in the picker since the 04th as an alternate, never made the
default despite being the actual fix for "lessac reads British." One
array reorder in `voiceClient.ts` (`DEFAULT_PIPER_VOICE_ID` derives
from array position), updated the handful of tests that were using
"switch to amy" as their non-default comparison case to use Lessac
instead so they still test a real change. 373 tests green, typecheck
clean.

Also updated CLAUDE.md's opening to drop the "designed as something
other people could use too" framing - Jeremy confirmed this is a
private, personal-only project now, matching the gamification
disclosure and the pending repo visibility flip.

Flipped the GitHub repo to private via `gh repo edit --visibility
private --accept-visibility-change-consequences`, at Jeremy's explicit
go-ahead after confirming my own access survives it (still
authenticated as the repo owner, so push/pull keeps working - verified
with a real `git fetch` right after the flip). `mycelia-work` is
private as of now.

## 18:32 — 11+11 plan dropped, hidden-unlock system removed entirely

Jeremy: with the revised asset pipeline, the whole 11+11 unlock plan is
no longer needed. Asked two follow-ups before touching anything - what
happens to the existing 18+ hidden-unlock mechanic itself, and the DLC
infinite-canvas idea it was going to gate. Answer on both: gone. Remove
the unlock entirely, scrap the DLC canvas concept - the new gamification
assets just live in the ordinary visible reward flow now, no secret
gate needed since the whole app's private anyway.

Pulled it all out: `HiddenUnlockPanel.tsx`, `rewards.rs` (password hash
+ local asset-serving commands), `rewardsClient.ts`, the Help menu's
"this should not be here" entry, the `rewardsUnlocked`/
`eighteenPlusEnabled` settings keys, and the now-dead `sha2`/`base64`
Rust crates. Updated the handed-off plan doc - Section 1 struck
through and marked cut, Phase 12's slot in the sequencing table marked
cut instead of renumbering everything else.

Full check before committing: typecheck clean, frontend suite 373 -> 353
(the 20 removed tests were this feature's own), `cargo build` and
`cargo test` clean (9 Rust tests, rewards.rs's 4 gone with the module),
lint clean. Committed as `d2e390e`, pushed.

## 18:48 — Phase 13 starts: gamification core (step 13.1, data layer)

Jeremy gave the go-ahead for Phase 13 (gamification core) and asked for
a full-app manual testing checklist once it's done, since he hasn't
tested the earlier phases and doesn't recall which ones he has. Working
this as several steps, chained without stopping until the whole phase
closes, per the usual process.

Step 13.1: three new tables - `gamification_stats` (singleton row),
`xp_events` (audit log, every XP award), `unlocked_achievements`
(one-time unlock records for level badges and the two streak-milestone
stickers only - repeatable stickers like project-finished and
welcome-back just get logged as xp_events with a sticker_key, no
separate unlock record needed since they can fire more than once).
Modeled `streak_days` as a cumulative count of distinct active days
rather than a literal consecutive-day streak - mathematically the same
thing once you accept the no-punishment rule ("pause, don't reset"),
and much simpler to implement/test correctly.

12 new repository tests, schema test extended, typecheck clean.
Committed as `045dee7`, pushed.

Step 13.2: the pure-function core in `src/services/gamification.ts` -
XP constants, the level curve, badge cadence, welcome-back pool, date
helpers. Verifying the level curve by actual computation (not mental
math this time) turned up a real error in the plan doc's own sample
table from the planning session - the numbers next to the formula
didn't match the formula. Formula itself (`100*(N-1)^1.6`) is unchanged
and still what Jeremy confirmed; just the illustrative numbers were
wrong. Fixed the table in the plan doc with a correction note. 24 new
tests, typecheck clean. Committed as `b9718d6`, pushed.
